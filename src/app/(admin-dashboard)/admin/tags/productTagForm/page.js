import TagsList from '@/components/(admin)/tags/productTagForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <TagsList/>
        </div>
    );
};

export default page;