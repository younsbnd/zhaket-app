import EditTagPage from '@/components/(admin)/tags/[id]/EditProductCategoryLogic';
import React from 'react';

const page = () => {
    return (
        <div>
            <EditTagPage />
        </div>
    );
};

export default page;